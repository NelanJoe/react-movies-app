import Layout from "../layouts/layout";

const Login = () => {
  return (
    <Layout>
      <section className="mt-12 font-semibold text-center space-y-4 h-screen grid place-content-center">
        <h1 className="text-3xl font-semibold italic">Login Page</h1>
        <p className="text-base opacity-50">Comingsoon...</p>
      </section>
    </Layout>
  );
};

export default Login;
