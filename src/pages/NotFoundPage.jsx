import Layout from "../layouts/layout";

const NotFoundPage = () => {
  return (
    <Layout>
      <section className="mt-12 font-semibold text-center">
        <h1 className="text-8xl font-semibold text-red-500">404 Not Found</h1>
      </section>
    </Layout>
  );
};

export default NotFoundPage;
